# Build tools (must be first)
setuptools>=65.5.0
wheel>=0.38.0
pip>=22.3

# Pip configuration
--only-binary=all
--prefer-binary

# Core packages with Python 3.11 compatible versions
numpy>=1.24.0,<1.27.0
pandas>=2.0.0,<2.2.0

# Flask and extensions
Flask>=2.2.0,<2.4.0
Werkzeug>=2.2.0,<2.4.0
Jinja2>=3.1.0
MarkupSafe>=2.1.0
itsdangerous>=2.1.0
click>=8.1.0
blinker>=1.5.0

# Web server
gunicorn>=20.1.0

# Additional packages (add only what you need)
requests>=2.28.0
python-dateutil>=2.8.0
